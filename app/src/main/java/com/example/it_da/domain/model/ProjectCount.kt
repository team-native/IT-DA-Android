package com.example.it_da.domain.model

// Represents the user's shared project activity counts.
data class ProjectCount(
    val applyingCount: Int,
    val participatingCount: Int,
    val completedCount: Int
)
