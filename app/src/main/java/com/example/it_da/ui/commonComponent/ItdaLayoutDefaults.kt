package com.example.it_da.ui.commonComponent

import androidx.compose.ui.unit.dp

object ItdaLayoutDefaults {
    // Keeps scrollable screen content visible above the fixed bottom navigation bar.
    val BottomNavigationContentPadding = 80.dp

    // Provides the standard horizontal padding for form-style screens.
    val FormHorizontalPadding = 30.dp

    // Provides the short vertical gap used between closely related form fields.
    val ShortVerticalSpacing = 15.dp

    // Provides the long vertical gap used between separated screen sections.
    val LongVerticalSpacing = 30.dp

    // Separates a section heading from the content displayed below it.
    val SectionHeaderContentSpacing = 13.dp

    // Separates project cards displayed together inside a project section.
    val ProjectCardSpacing = 15.dp
}
